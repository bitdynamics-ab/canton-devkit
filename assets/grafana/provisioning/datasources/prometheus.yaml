# Grafana datasource provisioned at boot. Points at the
# sibling prometheus container on the per-instance docker network.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
